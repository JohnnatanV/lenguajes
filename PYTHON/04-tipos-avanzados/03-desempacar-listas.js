const listas = [1, 2, 3, 4, 5]

const [uno, dos, tres] = listas

console.log(uno)

const objeto = { nombre: 'Juan', apellido: 'Perez', edad: 30 }

const { nombre, apellido, edad } = objeto

console.log(nombre, apellido)
